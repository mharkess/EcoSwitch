DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `DesiredRangeUpdate`(IN `temp` INT(32), IN `ID` INT(32))
    MODIFIES SQL DATA
    SQL SECURITY INVOKER
    COMMENT 'Updates user''s desired temperature from app'
BEGIN
UPDATE ecoswitch.devicetemprecent
SET TargetTemp = temp
WHERE DeviceID = ID;
END$$
DELIMITER ;